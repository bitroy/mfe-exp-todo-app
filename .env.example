# Example environment variables for mfe-exp-todo-app
# This project serves a Module Federation script remote at /remoteEntry.js

# Port used by webpack dev server (default in this workspace)
# DEV_PORT=3002

# PUBLIC_PATH can be set if you host the app under a sub-path
# PUBLIC_PATH=/

# When consumed by other apps, the remote entry is available at:
# http://localhost:3002/remoteEntry.js
